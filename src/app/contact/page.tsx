import type { Metadata } from 'next';
import type { ReactElement } from 'react';

export const metadata: Metadata = {
  title: 'Contact — Farhad Esmaeili',
};

export default function ContactPage(): ReactElement {
  return <main />;
}
