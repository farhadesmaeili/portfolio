import type { Metadata } from 'next';
import type { ReactElement } from 'react';

export const metadata: Metadata = {
  title: 'Projects — Farhad Esmaeili',
};

export default function ProjectsPage(): ReactElement {
  return <main />;
}
