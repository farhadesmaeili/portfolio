import type { Metadata } from 'next';
import type { ReactElement } from 'react';

export const metadata: Metadata = {
  title: 'Blog — Farhad Esmaeili',
};

export default function BlogPage(): ReactElement {
  return <main />;
}
